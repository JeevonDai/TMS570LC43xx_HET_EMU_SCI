# TMS570 SRAM 固件加载器使用说明

## 概述

本方案实现了通过主 TMS570LC4357 芯片通过 GPIO 模拟 JTAG，将固件写入备用 TMS570LC4357 芯片的 SRAM 并启动运行。

## 架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                        主 TMS570LC4357                               │
├─────────────────────────────────────────────────────────────────────┤
│  FLASH BANK0 (0x00000000)    │  FLASH BANK1 (0x00200000)            │
│  ┌─────────────────────────┐ │  ┌─────────────────────────────────┐ │
│  │ 主 TMS570 程序代码      │ │  │ .sram_firmware 段               │ │
│  │ - 系统初始化            │ │  │ 备用芯片的 SRAM 版本固件数据   │ │
│  │ - JTAG GPIO 驱动        │ │  │                                 │ │
│  │ - SRAM 加载器           │ │  │                                 │ │
│  └─────────────────────────┘ │  └─────────────────────────────────┘ │
│                                                                      │
│  GPIO JTAG 信号:                                                     │
│  ┌──────────────────────────────────────────────────────────────────┐│
│  │ N2HET1_22 (TRST) ─────────────────────────────────────┐         ││
│  │ N2HET1_23 (TCK)  ─────────────────────────────────────┼─────┐   ││
│  │ N2HET1_24 (TDI)  ─────────────────────────────────────┼─────┼─┐ ││
│  │ N2HET1_29 (TMS)  ─────────────────────────────────────┼─────┼─┼─┐│
│  │ N2HET1_27 (TDO)  ◄────────────────────────────────────┼─────┼─┼─┼│
│  └───────────────────────────────────────────────────────┼─────┴─┴─┴│
└──────────────────────────────────────────────────────────│───────────┘
                                                           │
                                                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        备用 TMS570LC4357                             │
├─────────────────────────────────────────────────────────────────────┤
│  JTAG 端口: TRST, TCK, TDI, TDO, TMS                                │
│                                                                      │
│  SRAM (0x08000000 - 0x08080000, 512KB)                              │
│  ┌─────────────────────────────────────────────────────────────────┐│
│  │ 0x08000000: .intvecs (32 bytes) - 中断向量表                    ││
│  │ 0x08000020: .text    (~256KB)   - 代码段                        ││
│  │ 0x08040000: stacks   (~5KB)     - 栈区                          ││
│  │ 0x08041500: RAM      (~252KB)   - 数据区                        ││
│  └─────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────┘
```

## 文件结构

```
TMS570LC43xx_HET_EMU_SCI/
├── HALCoGen/
│   ├── include/
│   │   ├── jtag_gpio.h           # JTAG GPIO 接口定义
│   │   ├── sram_loader.h         # SRAM 加载器接口
│   └── source/
│       ├── jtag_gpio.c           # JTAG GPIO 实现
│       ├── sram_loader.c         # SRAM 加载器实现
│       ├── HL_sys_link.cmd       # FLASH 版本链接脚本
│       └── HL_sys_link_sram.cmd  # SRAM 版本链接脚本
├── build_sram_bin.bat            # SRAM 版本构建脚本
└── Debug_SRAM/                   # SRAM 版本输出目录（构建后生成）
    ├── TMS570LC43xx_SRAM.out
    ├── TMS570LC43xx_SRAM.bin
    └── TMS570LC43xx_SRAM.map
```

## 内存布局

### 主 TMS570（运行加载器）

| 区域 | 起始地址 | 大小 | 内容 |
|------|----------|------|------|
| VECTORS | 0x00000000 | 32B | 中断向量表 |
| FLASH0 | 0x00000020 | ~2MB | 主程序代码 |
| FLASH1 | 0x00200000 | 2MB | SRAM 固件数据 (.sram_firmware) |
| STACKS | 0x08000000 | ~5KB | 栈区 |
| RAM | 0x08001500 | ~507KB | 数据区 |

### 备用 TMS570（运行 SRAM 版本）

| 区域 | 起始地址 | 大小 | 内容 |
|------|----------|------|------|
| VECTORS | 0x08000000 | 32B | 中断向量表 |
| CODE | 0x08000020 | ~256KB | 代码和只读数据 |
| STACKS | 0x08040000 | ~5KB | 栈区 |
| RAM | 0x08041500 | ~252KB | 数据区 |

## 工作原理

### 1. JTAG 连接流程

```
主 TMS570                          备用 TMS570
    │                                    │
    │──── JTAG Reset ───────────────────►│ Test-Logic-Reset
    │                                    │
    │──── ICEPick Connect ──────────────►│ 建立调试连接
    │                                    │
    │──── Route SDTAP0 (DAP) ───────────►│ 路由到 ARM DAP
    │                                    │
    │──── DAP Power Up ─────────────────►│ 使能调试电源
    │                                    │
    │──── Select MEM-AP ────────────────►│ 选择内存访问端口
    │                                    │
```

### 2. 固件写入流程

```
主 TMS570                          备用 TMS570 SRAM
    │                                    │
    │──── Halt CPU ─────────────────────►│ CPU 挂起
    │                                    │
    │  ┌─────────────────────────────┐   │
    │  │ for each 32-bit word:      │   │
    │  │   Write TAR (address)      │───►│ [addr] = data
    │  │   Write DRW (data)         │   │
    │  └─────────────────────────────┘   │
    │                                    │
    │──── Set PC = 0x08000000 ──────────►│
    │                                    │
    │──── Resume CPU ───────────────────►│ CPU 从 SRAM 启动
    │                                    │
```

### 3. CPU 启动流程

1. 通过 ITR (Instruction Transfer Register) 执行调试指令
2. 将入口地址写入 DTRRX
3. 执行 `MRC p14,0,R0,c0,c5,0` 将地址移到 R0
4. 执行 `MOV PC, R0` 设置 PC
5. 写 DRCR 发送 RESTART 请求

## GPIO 引脚映射

| JTAG 信号 | N2HET1 引脚 | 方向 | 说明 |
|-----------|-------------|------|------|
| TRST | N2HET1_22 | 输出 | 测试逻辑复位 |
| TCK | N2HET1_23 | 输出 | 测试时钟 |
| TDI | N2HET1_24 | 输出 | 测试数据输入 |
| TDO | N2HET1_27 | 输入 | 测试数据输出 |
| TMS | N2HET1_29 | 输出 | 测试模式选择 |

## 错误码

| 错误码 | 值 | 说明 |
|--------|-----|------|
| SRAM_LOADER_OK | 0 | 操作成功 |
| SRAM_LOADER_ERR_CONNECT | 1 | JTAG 连接失败 |
| SRAM_LOADER_ERR_POWERUP | 2 | DAP 上电失败 |
| SRAM_LOADER_ERR_HALT | 3 | CPU 挂起失败 |
| SRAM_LOADER_ERR_WRITE | 4 | 写入失败 |
| SRAM_LOADER_ERR_VERIFY | 5 | 验证失败 |
| SRAM_LOADER_ERR_RESUME | 6 | CPU 恢复失败 |
| SRAM_LOADER_ERR_PARAM | 7 | 参数错误 |

## 性能参考

- JTAG 时钟频率：取决于 GPIO 操作延时，约 100-500 kHz
- 64KB 固件写入时间：约 5-10 秒（取决于 JTAG 速度）
- 完整加载流程：约 10-20 秒

## 注意事项

1. **电源时序**：确保备用芯片已正确上电后再进行 JTAG 操作
2. **JTAG 信号完整性**：GPIO 模拟 JTAG 速度较慢，注意信号质量
3. **大端序**：TMS570 使用大端序，写入数据时注意字节顺序
4. **中断向量表**：SRAM 版本的向量表必须位于 0x08000000
5. **调试冲突**：加载过程中不要连接其他调试器

## 常见问题

### Q: 程序加载后不运行
A: 检查入口地址是否正确（应为 0x08000000），检查向量表格式

### Q: 验证失败
A: 检查大端序转换是否正确，检查目标地址是否在有效 SRAM 范围内

