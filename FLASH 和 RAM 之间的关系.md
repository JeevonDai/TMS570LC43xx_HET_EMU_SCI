## 程序启动和数据搬运流程

### 📌 重要澄清：代码本身**不搬运**到 RAM

根据您当前使用的链接器脚本 `HL_sys_link.cmd`，**代码是直接从 Flash 执行的**（这叫做 XIP - Execute In Place），并没有搬运到 RAM。

**只有初始化数据（.data 段）才会从 Flash 复制到 RAM。**

---

## 完整启动流程

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. 上电复位                                                      │
│    CPU 从地址 0x00000000 开始执行                                │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. 中断向量表 (.intvecs) @ 0x00000000                           │
│    b _c_int00  ──→  跳转到启动函数                              │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. _c_int00() 启动函数 @ 0x00007CB0 (在Flash中执行)             │
│    ├─ _coreInitRegisters_()    // 初始化核心寄存器              │
│    ├─ _coreInitStackPointer_() // 初始化栈指针                  │
│    ├─ _memInit_()              // 初始化 L2RAM (ECC)            │
│    ├─ systemInit()             // 初始化时钟、Flash等           │
│    ├─ vimInit()                // 初始化中断                    │
│    ├─ __TI_auto_init()   ←──── ** 关键：数据搬运在这里！**       │
│    └─ main()                   // 调用用户程序                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## `__TI_auto_init()` 数据搬运详解

这是 TI 运行时库函数，负责 C 环境初始化。根据您的 map 文件：

```106:113:c:\Users\d00886377\Desktop\TMS570LC43xx_HET_EMU_SCI\Debug\TMS570LC43xx_HET_EMU_SCI.map
.cinit     0    00008920    00000030     
                  00008920    0000000c     (__TI_handler_table)
                  0000892c    00000004     --HOLE-- [fill = 0]
                  00008930    00000008     (.cinit..bss.load) [load image, compression = zero_init]
                  00008938    00000007     (.cinit..data.load) [load image, compression = lzss]
```

### 搬运过程：

| 段 | 加载地址 (Flash) | 运行地址 (RAM) | 大小 | 处理方式 |
|---|---|---|---|---|
| `.bss` | 0x00008930 | 0x08001500 | 80 bytes | **zero_init** - 清零，不复制 |
| `.data` | 0x00008938 | 0x08001550 | 4 bytes | **LZSS 解压** - 从 Flash 解压到 RAM |

### 搬运机制：

```c
// __TI_auto_init() 内部逻辑（简化）
void __TI_auto_init(void) {
    // 1. 读取 __TI_cinit_table (在 Flash 0x00008940)
    
    // 2. 对于 .bss 段：调用 __TI_zero_init
    //    将 RAM 0x08001500 开始的 80 bytes 清零
    memset(0x08001500, 0, 80);
    
    // 3. 对于 .data 段：调用 __TI_decompress_lzss
    //    从 Flash 0x00008938 解压数据到 RAM 0x08001550
    decompress_lzss(0x00008938, 0x08001550);
    
    // 4. 调用 C++ 构造函数（如果有）
}
```

---

## 内存布局总结

```
                    Flash (执行代码)                          RAM (运行数据)
              ┌─────────────────────┐                   ┌─────────────────────┐
  0x00000000  │ .intvecs (32B)      │                   │                     │
              ├─────────────────────┤     复制/清零      │                     │
  0x00000020  │ .text (~34KB)       │ ◄── 直接执行 ──►  │                     │
              │ (程序代码)          │                   │                     │
              ├─────────────────────┤                   │                     │
  0x000085C0  │ .const (836B)       │ ◄── 直接读取 ──►  │                     │
              ├─────────────────────┤                   ├─────────────────────┤
  0x00008920  │ .cinit (初始化表)   │ ═══════════════►  │ .bss (80B) 清零     │ 0x08001500
              │  └─ .bss.load       │      搬运          │                     │
              │  └─ .data.load      │ ═══════════════►  │ .data (4B) 解压     │ 0x08001550
              └─────────────────────┘                   └─────────────────────┘
```

---

## 如果要代码在 RAM 中执行？

您项目中有 `HL_sys_link_sram.cmd`，它将代码也放在 SRAM 中：

```65:77:c:\Users\d00886377\Desktop\TMS570LC43xx_HET_EMU_SCI\HALCoGen\source\HL_sys_link_sram.cmd
MEMORY
{
/* USER CODE BEGIN (2) */
/* USER CODE END */
    /* 所有内容都放在SRAM中 */
    VECTORS (X)  : origin=0x08000000 length=0x00000020
    CODE    (RX) : origin=0x08000020 length=0x0003FFE0
    STACKS  (RW) : origin=0x08040000 length=0x00001500
    RAM     (RW) : origin=0x08041500 length=0x0003EB00

/* USER CODE BEGIN (3) */
/* USER CODE END */
}
```

这种配置用于 **JTAG 调试**，调试器直接将程序加载到 SRAM，不需要写入 Flash。

---

## 总结

| 问题 | 答案 |
|---|---|
| 代码从 Flash 搬运到 RAM 吗？ | **否**，代码直接在 Flash 中执行 (XIP) |
| 什么会被搬运到 RAM？ | 只有 `.data` 段（已初始化的全局变量） |
| 谁负责搬运？ | `__TI_auto_init()` 函数（TI 运行时库） |
| 什么时候搬运？ | `_c_int00()` 启动函数中，调用 `main()` 之前 |