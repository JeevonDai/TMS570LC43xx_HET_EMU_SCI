下面是你给出的**整段流程的完整准确中文翻译**，保持忠实于原文、适合你用于调试 TMS570/TMS570LC 时理解 ARM Debug 操作序列。

---

# 📘 **将 0x11111111 写入系统内存地址 0x08000000 的流程 — 中文翻译**

写入系统内存地址 `0x08000000` 的值 `0x11111111`。

图 13 展示了从 JTAG-DP → APB-AP → CPU 调试组件的数据流以及相关寄存器（按编程模型视角）。为了简单，示意图将所有系统资源（Flash、SRAM、外设）都看作连接在 CPU 的互联结构上。此图并非 Hercules 设备实际的总线结构，具体实现请参考器件数据手册和 TRM。
该图仅展示 CPU 和 DAP 在编程模型上的关系。

要让 CPU 访问系统资源（读写内存），外部工具必须将 ARM 指令强制注入到 CPU 的流水线中。
本例要实现：

```
STR R0, [R1]      ; R0 = 0x11111111
                   ; R1 = 0x08000000
```

为了完成该写操作，需要三个高层步骤：

1. 将 R0 加载为 `0x11111111`
2. 将 R1 加载为 `0x08000000`
3. 执行 `STR R0, [R1]`

下面展示了如何操作，而不再重复 TAP 状态机的细节（这些细节在 2.3.3 节已描述）。
需要使用到 CPU 调试组件中的几个寄存器，如表 5 所示。

---

# 📋 **表 5：Cortex-R 调试内存映射寄存器**

| Offset         | 名称                                                            | 描述 |
| -------------- | ------------------------------------------------------------- | -- |
| **0x80 DTRRX** | Host 写入的数据寄存器。CPU 执行 `MRC p14,0,<Rd>,c0,c5,0` 将 DTRRX 内容读入 Rd |    |
| **0x84 ITR**   | 调试器馈送指令给 CPU 的入口                                              |    |
| **0x88 DSCR**  | 调试状态与控制寄存器，用于判断读写状态、指令完成状态等                                   |    |
| **0x8C DTRTX** | CPU 写出的数据寄存器。CPU 执行 `MCR p14,0,<Rd>,c0,c5,0` 写入               |    |
| **0x90 DRCR**  | 用于让 CPU 进入或退出 Debug 状态                                        |    |

---

# 🚀 **写入流程步骤**

---

## **1. 激活 APB-AP**

* 向 **JTAG-DP.ADDR** 写 `0x01000000`
  → 选择 APB-AP，并选择它的 bank0。

---

## **2. 设置 APB-AP 访问地址（指向 DTRRX）**

* 向 **APB-AP.TAR** 写 `0x80001080`。
  这是 CPU 调试组件中 **DTRRX** 的地址。

说明：

* CoreSight debug 组件都按 ROM table 定址。
* ROM table 基地址 = **0x80000000**
* ARM 内核调试组件 offset = **0x1000**
* ARM core base = `0x80000000 + 0x1000 = 0x80001000`
* DTRRX offset = `0x80`
* 最终地址：`0x80001080`

---

## **3. 设置 APB-AP 数据（写入 0x11111111 到 DTRRX）**

* 向 **APB-AP.DRW** 写 `0x11111111`
  该值会通过 Debug APB bus 写入 CPU 的 DTRRX 寄存器。

---

## **4. 执行 CPU 指令，将 DTRRX → R0**

步骤：

1. 写 `0x80001084` → APB-AP.TAR（指向 ITR）
2. 写指令 opcode → APB-AP.DRW
   `MRC p14,0,r0,c0,c5,0`
   Opcode = **0xEE100E15**

CPU 执行该指令后，R0 中将包含 `0x11111111`。

---

## **5. 设置 APB-AP 地址（指向 DTRRX）**

* 再写一次 `0x80001080` → APB-AP.TAR

---

## **6. 设置 APB-AP 数据（写入地址 0x08000000 到 DTRRX）**

* 向 **APB-AP.DRW** 写 `0x08000000`

该值将被写入 CPU 的 DTRRX。

---

## **7. 执行 CPU 指令，将 DTRRX → R1**

步骤：

1. 写 `0x80001084` → APB-AP.TAR（指向 ITR）
2. 写 opcode → APB-AP.DRW：
   `MRC p14,0,r1,c0,c5,0`
   Opcode = **0xEE101E15**

执行完成后，R1 = `0x08000000`。

---

## **8. 设置 APB-AP 地址（指向 ITR）**

* 写 `0x80001084` → APB-AP.TAR

---

## **9. 写入 STR 指令以执行内存写入**

* 向 **APB-AP.DRW** 写 opcode：
  `STR R0, [R1]`
  Opcode = **0xE5810000**

CPU 执行该指令，会将 R0（0x11111111）写入内存地址 R1（0x08000000）。

---

# ⚠ 注意事项

以上步骤省略了对状态的检查。正确实现流程时应检查：

* **DTRRXfull**   — DTRRX 是否可写
* **DTRTXfull**   — 是否有数据可读
* **InstrCompl**  — 指令是否执行完成

这些状态位在 **DSCR** 寄存器中。

外部工具也可以参考 2.3.4 节，通过写 DRCR 来让 CPU 进入 Debug 状态。

---

# 📌 如需我可以继续提供：

✔ 完整 TAP 序列（JTAG_Shift_Bit 调用序列）
✔ 简化版流程图
✔ 自动化写内存脚本（C / Python）
✔ Ghidra / OpenOCD 中对应的 Debug 操作映射

告诉我你需要哪个！
